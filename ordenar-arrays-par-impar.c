/* Dado un array de 10 numeros enteros, retornar el mismo array ordenado, en los
 * primeros lugares los pares y despues los impares.
 */
#include <stdio.h>

int main() {
  int numeros[] = {1, 10, 3, 20, 13, 7, 9, 30, 41, 50};

  while () {
    /* code */
  }

  return 0;
}