// ARRAYS - arreglos - vectores - matrices unidimesionales
#include <stdio.h>
#define N 100

int main()

{
  int notas[N];
  int i;

  for (i = 0; i < N; i++) {
    notas[i] + i + 1;
  }

  for (int i = 0; i < N; i++) {
    printf("%d ", notas[i]);
  }
  return 0;
}